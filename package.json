{
  "name": "ow-server-only",
  "version": "1.0.0",
  "description": "OrderWater server only",
  "main": "start-server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3"
  }
}
